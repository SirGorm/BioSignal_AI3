# Library-first refactor extras. Install on top of the project's existing
# torch + pandas + numpy + scipy + sklearn stack (see pyproject.toml).
lightning>=2.2
torchmetrics>=1.3
libemg>=1.0
neurokit2>=0.2.7
antropy>=0.1.6
omegaconf>=2.3
pytorch-tcn>=1.2
optuna>=3.5
